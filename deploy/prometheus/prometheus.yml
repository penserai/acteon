global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "acteon"
    metrics_path: "/metrics/prometheus"
    static_configs:
      - targets: ["acteon:8080"]
        labels:
          service: "acteon-gateway"
