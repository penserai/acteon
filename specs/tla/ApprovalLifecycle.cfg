\* ApprovalLifecycle model-checking configuration (gateway approve/reject path)
CONSTANTS
    Approvers = {a1, a2}
    Rejecters = {r1}
    TTL = 2
    MaxTime = 4
    NOBODY = NOBODY

SPECIFICATION Spec

INVARIANTS
    TypeOK
    ExecuteOnceIfApproved
    IntentBeforeExecute
    DecidedOnce
