rules:
  # Catch-all: suppress any action not matched by higher-priority rules
  - name: deny-unmatched
    priority: 100
    description: "Block any action not matched by a higher-priority rule"
    condition:
      field: action.tenant
      eq: "acme-store"
    action:
      type: suppress
