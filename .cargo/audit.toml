[advisories]
# sqlx's lockfile contains its optional MySQL/RSA dependency graph. Acteon only
# compiles PostgreSQL; scripts/ci/security.sh verifies RSA remains unreachable.
# No RSA private-key operation is authorized by this exception.
ignore = ["RUSTSEC-2023-0071"]
